# Database connection path or URL (defaults to adaptive_content.db)
DATABASE_URL=adaptive_content.db

# AI Provider ("mock", "openai", "anthropic", "gemini")
AI_PROVIDER=mock

# AI API Key (required only if AI_PROVIDER is set to an external service)
AI_API_KEY=

# Application Environment ("development", "production", "test")
ENVIRONMENT=development
